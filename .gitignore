# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test runner output (generated)
test-results
playwright-report

# Waitlist data (Phase 7) — never commit collected user emails
data/waitlist.json
data/waitlist.json.tmp
data/*.backup.json

# Server-side secrets (OpenAI key, etc.) — never commit. `.env.example` is the safe template.
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
