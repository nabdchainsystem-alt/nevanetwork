# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test runner output (generated)
test-results
playwright-report

# Backend data (Phase 7 waitlist + Phase 8 invites/feedback/analytics) — never commit user data
data/waitlist.json
data/feedback.json
data/analytics.json
data/*.backup.json
data/*.bak
data/*.json.tmp

# Server-side secrets (OpenAI key, etc.) — never commit. `.env.example` is the safe template.
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
